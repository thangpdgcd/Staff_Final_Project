## components/common

Shared, non-primitive components (composition over raw UI primitives).

- Keep primitives in `src/components/ui/`
- Keep feature-specific components close to their pages when it helps readability

